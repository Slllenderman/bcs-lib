import React from 'react'
import { Box } from "@mui/material";

export const Info = () => {
    const info = {
        borderTop: "solid #CFCFCF 1px",
        borderBottom: "solid #CFCFCF 1px",
        padding: "56px 0px"
    }
    return (
        <Box sx={info}>
            {/* здесь через map пробрасывается info */}
            sdsvfcsdvdfvfv
        </Box>
    );
}