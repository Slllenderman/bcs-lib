import React from 'react';
import { Box, Typography } from '@mui/material';
import { props as iprops } from '@bauman-conference-library/interface'
export const Card = (props: iprops.ConferenceUserCard) => {
    return (
        <Box>
            
        </Box>
    )
}